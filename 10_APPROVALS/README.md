# Approvals Documentation

- PROPOSED tasks require explicit Ayush approval.
- Approved tasks are stored under `APPROVED`.
- Rejected tasks are stored under `REJECTED`.
- AI cannot approve its own proposals.
- Approval records must remain traceable.
- Approval records must not contain secrets.
