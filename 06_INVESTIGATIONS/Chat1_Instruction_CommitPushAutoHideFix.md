# Instruction — Commit + Push Auto-Hide Reservation Fix

Chat #1 | Node: Bug Fix — Auto-Hide Reservation Requests (commit/push)

## Task

Commit and push the already-implemented and build-verified auto-hide reservation requests fix directly to `main`.

## Steps

1. Confirm currently on `main` branch (`git branch --show-current`).
2. `git status` / `git diff` to review exactly what changed (`app/dashboard/tables/page.tsx`, `types/index.ts`).
3. Stage and commit with a message describing the change (e.g. "fix: auto-hide completed/expired reservation requests from dashboard panel").
4. Push to origin `main`.
5. Report: commit hash, push success/failure, files included in the commit.

## Do Not

- Do not start Track B work.
- Do not modify any files beyond what's already implemented.
