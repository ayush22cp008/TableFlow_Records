Chat #2 | Node 2 | Investigation Only — Staff Role Schema

Task: Report current schema for the profiles/users table's role/type column.

Report back with:
1. Exact table name
2. Exact column name for role/type
3. Exact column type (enum, text, check constraint, etc.) and its exact definition/allowed values
4. Any existing RLS policies referencing this column (list policy names + table + using/with-check expressions)
5. Any foreign key or trigger tied to this column
6. Sample of current distinct values in use (SELECT DISTINCT on that column)

No code changes. Investigation only — output findings as a report.
